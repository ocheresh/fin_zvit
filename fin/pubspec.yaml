name: fin
description: "A new Flutter prt."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  excel: ^4.0.0
  path_provider: ^2.1.0
  file_saver: ^0.2.0
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  provider: ^6.1.2
  dropdown_button2: ^2.3.9
  universal_html: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/references.json
    - assets/fin_rows.json
    - assets/proposal_calcs.json
